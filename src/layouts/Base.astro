---
import BaseHead from "@/components/BaseHead.astro";
import SkipLink from "@/components/SkipLink.astro";
import StructuredData from "@/components/StructuredData.astro";
import ThemeProvider from "@/components/ThemeProvider.astro";
import Footer from "@/components/layout/Footer.astro";
import Header from "@/components/layout/Header.astro";
import { siteConfig } from "@/site.config";
import type { SiteMeta } from "@/types";

import type { LayoutStructuredData } from '@/types';

interface Props {
	meta: SiteMeta;
	structuredData?: LayoutStructuredData[];
}

const {
	meta: { articleDate, description = siteConfig.description, ogImage, title, hasMath },
	structuredData = []
} = Astro.props;

// Determine if this is the homepage
---

<html
	class="overflow-x-hidden grid scroll-pt-space-xl scroll-smooth font-sans text-textColor text-xl md:text-base antialiased"
	lang={siteConfig.lang}
>
	<head>
		<BaseHead articleDate={articleDate} description={description} ogImage={ogImage} title={title} hasMath={hasMath} />
		{/* Website structured data for all pages */}
		<StructuredData type="website" />
		{/* Additional structured data passed from child layouts */}
		{structuredData.map((sd: LayoutStructuredData) => (
			<StructuredData type={sd.type} data={sd.data} />
		))}
	</head>
	<body class="relative m-auto min-h-screen w-full max-w-6xl grow bg-bgColor">
		<ThemeProvider />
		<SkipLink />

		<!-- Simplified background - removed decorative gradient orbs for minimal aesthetic -->
	
		<div class="relative flex min-h-screen w-full">
			<!-- Sidebar for Docs Series -->
			<slot name="sidebar" />
			<!-- Main container for the page content -->
			<div id="container" class="relative m-auto max-w-4xl grow">
				<div class="m-auto grid min-h-screen md:grid-rows-[auto_auto_1fr] px-component md:pt-component">
					<Header />
					<main id="main" class="relative flex-grow mt-space-xl md:mt-space-m">
						<slot />
					</main>
					<Footer />
				</div>
			</div>
		</div>
	</body>
</html>
